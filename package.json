{
  "name": "chartjs-example",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "predev": "rm -rf docs",
    "prebuild": "rm -rf docs prs_stats",
    "dev": "parcel -p 8000 --dist-dir docs src/index.html",
    "help": "parcel help",
    "build": "parcel build --public-url /prs_stats --dist-dir docs src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "@sgratzl/chartjs-chart-boxplot": "^4.1.1",
    "chart.js": "^4.2.1",
    "chartjs-plugin-zoom": "^2.0.1"
  }
}
